on: 
  workflow_dispatch:
  
      jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: Run a one-line script
          run: API OpenWeather!
          
          
    deploy-qa:
      runs-on: ubuntu-latest
      environment:
      name: qa
      url: 'diegothainan.com'
    needs: build
          steps:
        - name: Run a one-line script
          run: API OpenWeather QA!
          
          
